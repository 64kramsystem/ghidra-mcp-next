<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.xebyte</groupId>
    <artifactId>GhidraMCP-next</artifactId>
    <packaging>jar</packaging>
    <version>5.15.0</version>
    <name>GhidraMCP-next</name>
    <description>Production-ready Model Context Protocol server for Ghidra reverse engineering platform. Provides MCP-based tooling and automation for reverse engineering workflows: convention-enforcing rename/type/comment endpoints, batch operations, P-code emulation, live debugger integration, headless server, and Ghidra Server multi-version support.</description>
    <url>https://github.com/64kramsystem/ghidra-mcp-next</url>
    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <ghidra.version>12.1.2</ghidra.version>
        <mockito.version>5.23.0</mockito.version>
        <!-- Build timestamp generated at compile time (format: yyyyMMdd-HHmmss) -->
        <build.timestamp>${maven.build.timestamp}</build.timestamp>
        <maven.build.timestamp.format>yyyyMMdd-HHmmss</maven.build.timestamp.format>
        <!-- Build number - use current time as unique identifier -->
        <build.number>${maven.build.timestamp}</build.number>
        <!-- Default empty so surefire's @{argLine} resolves even when the
             JaCoCo agent is skipped (jacoco.skip=true); prepare-agent
             overwrites this property at runtime. -->
        <argLine></argLine>
    </properties>
    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>
    <developers>
        <developer>
            <id>bethington</id>
            <name>Project Maintainer</name>
            <url>https://github.com/bethington</url>
        </developer>
    </developers>
    <scm>
        <connection>scm:git:https://github.com/64kramsystem/ghidra-mcp-next.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/64kramsystem/ghidra-mcp-next.git</developerConnection>
        <url>https://github.com/64kramsystem/ghidra-mcp-next/tree/main</url>
    </scm>
    <dependencies>
        <!-- Ghidra dependencies (installed locally via mvn install:install-file) -->
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>Generic</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>SoftwareModeling</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>Project</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>Docking</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>Decompiler</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>Utility</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>Base</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>Gui</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>FileSystem</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>Help</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <!-- Ghidra Emulation (for EmulationService P-code emulator) -->
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>Emulation</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <!-- Ghidra Debug module dependencies (for DebuggerService) -->
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>Debugger-api</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>Framework-TraceModeling</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>Debugger-rmi-trace</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>ProposedUtils</artifactId>
            <version>${ghidra.version}</version>
        </dependency>
        <!-- Gson (provided — bundled with Ghidra in Framework/Generic/lib/) -->
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>2.14.0</version>
            <scope>provided</scope>
        </dependency>
        <!-- ProjectLocator uses this Ghidra-bundled library at runtime. -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>3.20.0</version>
            <scope>provided</scope>
        </dependency>
        <!-- JUnit (test only) -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
        <!-- Mockito (test only) -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <!-- DataType initialization reaches Ghidra's SettingsImpl, whose
             NO_SETTINGS singleton depends on the JavaHelp API at test time. -->
        <dependency>
            <groupId>javax.help</groupId>
            <artifactId>javahelp</artifactId>
            <version>2.0.05</version>
            <scope>test</scope>
        </dependency>
        <!-- Ghidra DB (test only) — needed so Mockito can instrument Program's type hierarchy -->
        <dependency>
            <groupId>ghidra</groupId>
            <artifactId>DB</artifactId>
            <version>${ghidra.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <finalName>${project.artifactId}-${project.version}</finalName>
        <!-- Enable resource filtering for version substitution -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>com/xebyte/version.properties</include>
                    <include>extension.properties</include>
                </includes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <excludes>
                    <exclude>com/xebyte/version.properties</exclude>
                    <exclude>extension.properties</exclude>
                </excludes>
            </resource>
        </resources>
        <plugins>
            <!-- Maven Surefire Plugin — load Mockito statically, disable CDS (instrumentation
                 appends to the bootstrap classpath), and enable Byte Buddy experimental mode
                 for Java 25. @{argLine} late-binds the JaCoCo agent. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.6</version>
                <configuration>
                    <argLine>@{argLine} -javaagent:${settings.localRepository}/org/mockito/mockito-core/${mockito.version}/mockito-core-${mockito.version}.jar -Xshare:off -Dnet.bytebuddy.experimental=true</argLine>
                </configuration>
            </plugin>
            <!-- JaCoCo — line coverage for the offline test tier. Report lands in
                 target/site/jacoco/ on every `mvn test`. Baseline-only for now
                 (no coverage gate); disable with -Djacoco.skip=true. -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.15</version>
                <executions>
                    <execution>
                        <id>jacoco-prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jacoco-report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- Maven Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <encoding>UTF-8</encoding>
                    <showWarnings>true</showWarnings>
                    <compilerArgs>
                        <arg>-Xlint:all</arg>
                        <arg>-Werror</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <!-- Ensure deterministic resource/properties encoding -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <propertiesEncoding>${project.build.sourceEncoding}</propertiesEncoding>
                </configuration>
            </plugin>
            <!-- Use custom MANIFEST.MF -->
            <plugin>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <archive>
                        <manifestFile>src/main/resources/META-INF/MANIFEST.MF</manifestFile>
                    </archive>
                    <!-- Exclude the App class -->
                    <excludes>
                        <exclude>**/App.class</exclude>
                    </excludes>
                    <!-- Make sure output directory is target for consistency -->
                    <outputDirectory>${project.build.directory}</outputDirectory>
                </configuration>
            </plugin>
            <!-- The Assembly Plugin for creating the Ghidra extension ZIP -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.8.0</version>
                <configuration>
                    <!-- Using the custom assembly descriptor -->
                    <descriptors>
                        <descriptor>src/assembly/ghidra-extension.xml</descriptor>
                    </descriptors>
                    <!-- Don't append the assembly ID -->
                    <appendAssemblyId>false</appendAssemblyId>
                </configuration>
            </plugin>
            <!-- Copy dependencies to target/lib for the assembly -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.11.0</version>
                <executions>
                    <execution>
                        <id>copy-dependencies</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/lib</outputDirectory>
                            <includeScope>runtime</includeScope>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!-- Build Profiles -->
    <profiles>
        <!-- Opt-in runtime support for ProgramBuilder-backed live fixtures.
             Activate with -Dghidra.test.install.dir=/path/to/ghidra.  These
             jars are shipped by Ghidra but are not transitive dependencies of
             the locally installed module POMs.  Keeping them on Surefire's
             runtime path avoids changing normal compilation or CI behavior. -->
        <profile>
            <id>ghidra-live-fixtures</id>
            <activation>
                <property>
                    <name>ghidra.test.install.dir</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <additionalClasspathElements>
                                <!-- Application logging bootstrap. -->
                                <additionalClasspathElement>${ghidra.test.install.dir}/Ghidra/Framework/Generic/lib/log4j-api-2.25.4.jar</additionalClasspathElement>
                                <additionalClasspathElement>${ghidra.test.install.dir}/Ghidra/Framework/Generic/lib/log4j-core-2.25.4.jar</additionalClasspathElement>
                                <!-- Generic collections and XML support loaded by ProgramDB. -->
                                <additionalClasspathElement>${ghidra.test.install.dir}/Ghidra/Framework/Generic/lib/commons-collections4-4.1.jar</additionalClasspathElement>
                                <additionalClasspathElement>${ghidra.test.install.dir}/Ghidra/Framework/Generic/lib/commons-io-2.19.0.jar</additionalClasspathElement>
                                <additionalClasspathElement>${ghidra.test.install.dir}/Ghidra/Framework/Generic/lib/guava-32.1.3-jre.jar</additionalClasspathElement>
                                <additionalClasspathElement>${ghidra.test.install.dir}/Ghidra/Framework/Generic/lib/jdom2-2.0.6.1.jar</additionalClasspathElement>
                                <!-- Runtime-only framework modules reached during language initialization. -->
                                <additionalClasspathElement>${ghidra.test.install.dir}/Ghidra/Framework/Graph/lib/Graph.jar</additionalClasspathElement>
                                <additionalClasspathElement>${ghidra.test.install.dir}/Ghidra/Framework/Help/lib/javahelp-2.0.05.jar</additionalClasspathElement>
                                <!-- SLEIGH language-schema validation runtime. -->
                                <additionalClasspathElement>${ghidra.test.install.dir}/Ghidra/Framework/SoftwareModeling/lib/isorelax-20050913.jar</additionalClasspathElement>
                                <additionalClasspathElement>${ghidra.test.install.dir}/Ghidra/Framework/SoftwareModeling/lib/msv-20050913.jar</additionalClasspathElement>
                                <additionalClasspathElement>${ghidra.test.install.dir}/Ghidra/Framework/SoftwareModeling/lib/xsdlib-20050913.jar</additionalClasspathElement>
                                <additionalClasspathElement>${ghidra.test.install.dir}/Ghidra/Framework/SoftwareModeling/lib/relaxngDatatype-20050913.jar</additionalClasspathElement>
                                <additionalClasspathElement>${ghidra.test.install.dir}/Ghidra/Framework/SoftwareModeling/lib/antlr-runtime-3.5.2.jar</additionalClasspathElement>
                            </additionalClasspathElements>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Headless Server Profile -->
        <profile>
            <id>headless</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-jar-plugin</artifactId>
                        <version>3.5.0</version>
                        <configuration>
                            <archive>
                                <manifest>
                                    <mainClass>com.xebyte.headless.GhidraMCPHeadlessServer</mainClass>
                                    <addClasspath>true</addClasspath>
                                    <classpathPrefix>lib/</classpathPrefix>
                                </manifest>
                            </archive>
                            <outputDirectory>${project.build.directory}</outputDirectory>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <version>3.8.0</version>
                        <configuration>
                            <descriptors>
                                <descriptor>src/assembly/headless-jar.xml</descriptor>
                            </descriptors>
                            <appendAssemblyId>false</appendAssemblyId>
                        </configuration>
                        <executions>
                            <execution>
                                <id>make-headless-assembly</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

    </profiles>
</project>
